{
  "name": "moviebox-api",
  "version": "4.2.0",
  "description": "MovieBox REST API — auto-refreshing anonymous token, zero-buffer streaming",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev":   "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express":    "^4.19.2",
    "node-fetch": "^3.3.2"
  }
}
